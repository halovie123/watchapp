<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.watchapp">

    <!-- Quyền cảm biến -->
    <uses-permission android:name="android.permission.BODY_SENSORS" />
    <uses-permission android:name="android.permission.ACTIVITY_RECOGNITION" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Quyền INTERNET -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Bluetooth Permissions - Hỗ trợ cả Android 10 và Android 12+ -->
    <!-- Android 10 và thấp hơn dùng BLUETOOTH và BLUETOOTH_ADMIN -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Android 12+ (API 31+) dùng BLUETOOTH_SCAN và BLUETOOTH_CONNECT -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT"
        tools:targetApi="s" />

    <!-- Location - BẮT BUỘC cho BLE scan trên Android 10 -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Tính năng phần cứng -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />
    <uses-feature android:name="android.hardware.sensor.heartrate" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- Onboarding Activity (Main Launcher) -->
        <activity
            android:name=".OnboardingActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BLE Scan Activity -->
        <activity
            android:name=".BLEScanActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar"
            android:label="Quét thiết bị BLE" />

        <!-- Main Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Heart Rate Activity -->
        <activity
            android:name=".HeartRateActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Oxygen Activity -->
        <activity
            android:name=".OxygenActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Advanced Settings Activity -->
        <activity
            android:name=".AdvancedSettingsActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar" />

        <!-- Fall Detection Activity -->
        <activity
            android:name=".FallDetectionActivity"
            android:exported="false"
            android:theme="@style/AppTheme.NoActionBar"/>

        <!-- Chatbox Activity -->
        <activity
            android:name=".ChatboxActivity"
            android:exported="true"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- BLE Service -->
        <service
            android:name=".BLEService"
            android:enabled="true"
            android:exported="false" />

    </application>

</manifest>